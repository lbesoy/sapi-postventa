const axios = require('axios');
const fs = require('fs');

async function test() {
  try {
    const sapUrl = "https://189.196.226.242:50000/b1s/v1";
    // Need to login first or use existing session.
    // Let's just grep the actual response from the local server
  } catch (e) {
    console.log(e);
  }
}
test();
